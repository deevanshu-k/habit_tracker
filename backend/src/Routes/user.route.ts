import { Router } from "express";
const router = Router();

router.get("/", (req, res) => {});
router.patch("/", (req, res) => {});
router.post("/profileimage", (req, res) => {});

export default router;
